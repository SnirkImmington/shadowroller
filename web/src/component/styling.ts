import type { CSSObject } from 'styled-components/macro';

/** pointerStyling causes an element to display a pointer on mouseover. */
export const userPointer: CSSObject = {
    userSelect: "none",
    cursor: "pointer"
};
